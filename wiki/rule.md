## Ветки

Каждая задача выполняется в отдельной ветке.

Формат:

```
<type>/short-description
```

Типы:

* `feature` — новая функциональность
* `fix` — баг
* `refactor` — рефакторинг
* `docs` — документация
* `test` — тесты

Пример:

```
feature/add-search
fix/login-error
```

Прямые коммиты в `main` запрещены.

---

## Коммиты

Формат:

```
[feature/#1] - сообщение
```

Где:

* `feature` — тип
* `#1` — номер Issue

Примеры:

```
[feature/#12] - добавлен поиск по тегам
[fix/#8] - исправлена ошибка авторизации
```

Один коммит — одна логическая правка.
Сообщения должны быть осмысленными.

---

## Pull Request

* Работа через PR.
* Перед мержем — минимум 1 одобрение.
